[build]
builder = "NIXPACKS"
buildCommand = "cd backend && pip install -r requirements.txt && python create_model.py"

[deploy]
startCommand = "cd backend && python app.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "5000"
